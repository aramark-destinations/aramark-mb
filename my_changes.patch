diff --git a/brands/lake-powell/tokens.css b/brands/lake-powell/tokens.css
index 02636fb..bdfb018 100644
--- a/brands/lake-powell/tokens.css
+++ b/brands/lake-powell/tokens.css
@@ -7,11 +7,46 @@
    through CSS cascade specificity (loaded after styles.css).
 
    Only include tokens that DIFFER from root-tokens.css defaults.
+
+   Figma source: https://www.figma.com/design/sf2qQ3I3BFFN4NdWCC04cw/Lake-Powell-Site-Design
+   Node 48-127 "Text Styles" — typography analysis completed 2026-03-25
    ========================================================================== */
 
 :root {
-  /* Brand Colors — replace with actual Lake Powell brand values */
+  /* ========================================================================
+     Brand Colors
+     TODO: Verify final brand hex values with the Lake Powell brand guide.
+     Current values are placeholder — update with confirmed brand palette.
+     Figma text fill (fill-wyag1w) = rgba(3.5, 20.9, 38.3) ≈ #041526 = --color-grey-900 ✅
+     ======================================================================== */
   /* stylelint-disable-next-line color-hex-length */
   --color-primary: #0066cc;
   --color-secondary: #1a4d2e;
+
+  /* ========================================================================
+     Typography — Font Families
+     Lake Powell uses Montserrat (headings) and Inter (body) — same as global default.
+     Google Fonts import for Montserrat variable font (covers weight 100–900):
+     Add the following @import to this file once confirmed with the brand team,
+     to ensure Montserrat thin weights (100–250) load for .text-testimonial:
+
+     @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@100..700&display=swap');
+
+     Alternatively, add the woff2 file montserrat-latin-100to300.woff2 to /fonts/
+     and the @font-face declaration in styles/fonts.scss is already prepared.
+     ======================================================================== */
+
+  /* ========================================================================
+     Typography — Heading line-height override (if brand requires)
+     Global default: --heading-line-height: 1.3 (unitless, Figma-aligned)
+     No override needed for Lake Powell — inherits global default.
+     ======================================================================== */
+
+  /* ========================================================================
+     Typography — Body base size override (if brand requires)
+     Global default mobile: --body-font-size-m: 22px (intentionally larger than
+     Figma's 18px for improved mobile readability — documented deviation).
+     Global default desktop: --body-font-size-m: 18px (matches Figma Body 1).
+     No override needed for Lake Powell — inherits global default.
+     ======================================================================== */
 }
diff --git a/dist/styles/styles.css b/dist/styles/styles.css
index 6d5801f..6f6e38d 100644
--- a/dist/styles/styles.css
+++ b/dist/styles/styles.css
@@ -34,8 +34,8 @@
   --body-font-family: roboto, roboto-fallback, sans-serif;
   --heading-font-family: roboto-condensed, roboto-condensed-fallback, sans-serif;
     */
-  /* Assumeing same font family for all brands*/
-  --body-font-family: inter, inter-fallback, sans-serif;
+  /* Assumeing same font family for all brands */
+  --body-font-family: inter, inter-fallback, sans-serif;                                                              
   --heading-font-family: montserrat, montserrat-fallback, sans-serif;
   /* body sizes */
   --body-font-size-m: 22px;
diff --git a/styles/fonts.scss b/styles/fonts.scss
index 17d3eae..fac1aec 100644
--- a/styles/fonts.scss
+++ b/styles/fonts.scss
@@ -8,6 +8,7 @@
   unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
 }
 
+/* Montserrat Bold (700) — used for all headings H1–H4 and Display */
 @font-face {
   font-family: montserrat;
   font-style: normal;
@@ -17,3 +18,18 @@
   unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
 }
 
+/* Montserrat Thin (100–300) — used for Testimonial / Quote styles
+   Figma spec: Desktop Testimonial = weight 100, Mobile Testimonial = weight 250
+   TODO: Add montserrat-latin-100to300.woff2 to /fonts/ directory.
+   This file must cover the weight range 100–300 (variable font or separate thin woff2).
+   Until the file is added, the browser will fall back to montserrat-fallback (Arial).
+   Recommended source: https://fonts.google.com/specimen/Montserrat (download variable font)
+   or self-host the Google Fonts woff2 for weight range 100-300. */
+@font-face {
+  font-family: montserrat;
+  font-style: normal;
+  font-weight: 100 300;
+  font-display: swap;
+  src: url('../fonts/montserrat-latin-100to300.woff2') format('woff2');
+  unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+0304, U+0308, U+0329, U+2000-206F, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
+}
diff --git a/styles/styles.scss b/styles/styles.scss
index a326876..a2bdb7e 100644
--- a/styles/styles.scss
+++ b/styles/styles.scss
@@ -47,8 +47,29 @@
   --body-font-size-xs: 17px;
   --body-font-size-xxs: 12px; /* intentionally the same on desktop — used only for micro-text (blockquote small, badges) */
 
-  /* body line-height */
+  /* body line-height — Figma-aligned per size
+     Body 1 (18px) = 1.556 | Body 2 (16px) = 1.5 | Body 3 (14px) = 1.571 | Body 4 (12px) = 1.5
+     Note: --body-line-height-m uses 1.6 (slightly above Figma 1.556) for improved readability at larger mobile sizes */
   --body-line-height-m: 1.6;
+  --body-line-height-s: 1.5;
+  --body-line-height-xs: 1.571;
+  --body-line-height-xxs: 1.5;
+
+  /* body font weights — Figma-aligned (Light=300, Medium=500, Bold=700, Link/Regular=400) */
+  --body-font-weight-light: 300;
+  --body-font-weight-regular: var(--font-weight-normal);
+  --body-font-weight-medium: var(--font-weight-medium);
+
+  /* body letter-spacing — Figma-aligned: all body text uses -2% (-0.02em)
+     Source: Figma node 48-123 (all Body 1–4 Light/Medium/Bold/Link styles) */
+  --body-letter-spacing: -0.02em;
+
+  /* display / hero heading — Figma-aligned (node 48-127 style-6pqczy / style-qe69jc)
+     Desktop Display: Montserrat 700 / 56px / 1.2lh / -3% ls
+     Mobile Display:  Montserrat 700 / 32px / 1.2lh / -3% ls */
+  --display-font-size: 32px;  /* mobile first */
+  --display-line-height: 1.2;
+  --display-letter-spacing: -0.03em;
 
   /* TODO: remove legacy heading sizes and update blocks to use Figma-aligned heading sizes */
   --heading-font-size-xxl: 55px;
@@ -67,9 +88,10 @@
   --heading-font-size-h4: 20px;
 
   /* heading shared typography tokens — Figma-aligned
-     All headings: Montserrat 700, line-height 1.3em, letter-spacing -3%
-     Source: Figma node 48-124 */
-  --heading-line-height: 1.3em;
+     All headings: Montserrat 700, line-height 1.3 (unitless), letter-spacing -3%
+     Source: Figma node 48-124
+     Note: unitless 1.3 preferred over 1.3em to avoid line-height compounding in child elements */
+  --heading-line-height: 1.3;
   --heading-letter-spacing: -0.03em;
   --heading-font-weight: var(--font-weight-bold);
   --heading-margin-top: 0.8em;
@@ -87,6 +109,44 @@
   --list-letter-spacing: -0.02em;
   --list-padding-left: 1.5em;
 
+  /* eyebrow labels — Figma-aligned (node 142-269 styles style-6lht6m / style-d2quk5 / style-4i7b7p)
+     Inter 500 / UPPERCASE / +10% letter-spacing / line-height 1.0
+     Small=10px | Medium=14px | Large=16px */
+  --eyebrow-font-family: var(--body-font-family);
+  --eyebrow-font-weight: var(--font-weight-medium);
+  --eyebrow-font-size-s: 10px;
+  --eyebrow-font-size-m: 14px;
+  --eyebrow-font-size-l: 16px;
+  --eyebrow-line-height: 1;
+  --eyebrow-letter-spacing: 0.1em;
+  --eyebrow-text-transform: uppercase;
+
+  /* details / micro-text — Figma-aligned (node 4428-10016 styles style-90v097 / style-ziaf6d)
+     Inter / 10px / 1.3lh
+     Light=500 weight | Dark=700 weight */
+  --details-font-size: 10px;
+  --details-line-height: 1.3;
+  --details-font-weight-light: var(--font-weight-medium);
+  --details-font-weight-dark: var(--font-weight-bold);
+
+  /* testimonial / quote — Figma-aligned (node 5217-3113/3114 styles style-fout64 / style-9m7ucy)
+     Montserrat ultra-thin / no letter-spacing
+     Desktop: weight=100, 32px, lh=1.25 | Mobile: weight=250, 24px, lh=1.333
+     Note: requires Montserrat weight 100–300 woff2 to be loaded (see fonts.scss) */
+  --testimonial-font-family: var(--heading-font-family);
+  --testimonial-font-size: 24px;  /* mobile first */
+  --testimonial-font-weight: 250; /* ultra-thin — mobile Figma spec */
+  --testimonial-line-height: 1.333;
+
+  /* input typography — Figma-aligned (node 48-122 styles style-vw7hmy / style-eodh3q / style-3q0na5)
+     Inter / 16px for light+heavy | 14px for error */
+  --input-font-weight-light: var(--font-weight-normal);
+  --input-font-weight-heavy: var(--font-weight-medium);
+  --input-font-size-base: 16px;
+  --input-font-size-error: 14px;
+  --input-line-height-base: 1;
+  --input-line-height-error: 1.3;
+
   /* nav height */
   --nav-height: 64px;
 }
@@ -126,6 +186,10 @@
     --heading-font-size-s: 20px;
     --heading-font-size-xs: 18px;
 
+    /* display — Figma-aligned desktop (style-6pqczy)
+       Desktop Display: Montserrat 700 / 56px */
+    --display-font-size: 56px;
+
     /* heading sizes — Figma-aligned desktop
        Desktop: H1=40px, H2=32px, H3=24px, H4=20px
        Source: Figma node 48-124 styles a9apv0 / zcml4i / bzoiyn / t23uce */
@@ -133,6 +197,17 @@
     --heading-font-size-h2: 32px;
     --heading-font-size-h3: 24px;
     --heading-font-size-h4: 20px;
+
+    /* testimonial — Figma-aligned desktop (style-fout64)
+       Desktop: weight=100, 32px, lh=1.25 */
+    --testimonial-font-size: 32px;
+    --testimonial-font-weight: 100;
+    --testimonial-line-height: 1.25;
+
+    /* body sizes — desktop (Figma-aligned)
+       Figma Body 1=18px, Body 2=16px, Body 3=14px, Body 4=12px (same on desktop)
+       Note: desktop --body-font-size-m correctly matches Figma Body 1 (18px) */
+    --body-line-height-m: 1.556; /* desktop: tighter alignment with Figma Body 1 spec */
   }
 }
 
diff --git a/styles/typography.scss b/styles/typography.scss
index c5ce887..7424487 100644
--- a/styles/typography.scss
+++ b/styles/typography.scss
@@ -131,6 +131,7 @@ main {
 p {
     font-size: var(--body-font-size-m);
     line-height: var(--body-line-height-m);
+    letter-spacing: var(--body-letter-spacing);
     margin-top: 0;
     margin-bottom: 2.5rem;
 
@@ -161,7 +162,7 @@ ol {
     padding-left: var(--list-padding-left);
     font-size: var(--list-font-size);
     line-height: var(--list-line-height);
-    letter-spacing: var(--list-letter-spacing);
+    letter-spacing: var(--list-letter-spacing); /* = var(--body-letter-spacing) = -0.02em */
 
     >li {
         margin-top: 0;
@@ -208,6 +209,90 @@ dd {
     margin-left: 0;
 }
 
+/* ============================================================
+   Display / Hero heading — Figma-aligned (node 48-127 style-6pqczy / style-qe69jc)
+   Montserrat 700 / Mobile: 32px / Desktop: 56px / lh=1.2 / -3% ls
+   Use class .text-display on an element or heading to apply.
+   ============================================================ */
+.text-display {
+    font-family: var(--heading-font-family);
+    font-weight: var(--font-weight-bold);
+    font-size: var(--display-font-size);
+    line-height: var(--display-line-height);
+    letter-spacing: var(--display-letter-spacing);
+    overflow-wrap: anywhere;
+}
+
+/* ============================================================
+   Eyebrow labels — Figma-aligned (node 142-269)
+   Inter 500 / UPPERCASE / +10% letter-spacing / lh=1.0
+   Small=10px | Medium=14px | Large=16px
+   Use class .eyebrow or .eyebrow-s / .eyebrow-m / .eyebrow-l
+   ============================================================ */
+.eyebrow,
+.eyebrow-m {
+    font-family: var(--eyebrow-font-family);
+    font-weight: var(--eyebrow-font-weight);
+    font-size: var(--eyebrow-font-size-m);
+    line-height: var(--eyebrow-line-height);
+    letter-spacing: var(--eyebrow-letter-spacing);
+    text-transform: var(--eyebrow-text-transform);
+}
+
+.eyebrow-s {
+    font-family: var(--eyebrow-font-family);
+    font-weight: var(--eyebrow-font-weight);
+    font-size: var(--eyebrow-font-size-s);
+    line-height: var(--eyebrow-line-height);
+    letter-spacing: var(--eyebrow-letter-spacing);
+    text-transform: var(--eyebrow-text-transform);
+}
+
+.eyebrow-l {
+    font-family: var(--eyebrow-font-family);
+    font-weight: var(--eyebrow-font-weight);
+    font-size: var(--eyebrow-font-size-l);
+    line-height: var(--eyebrow-line-height);
+    letter-spacing: var(--eyebrow-letter-spacing);
+    text-transform: var(--eyebrow-text-transform);
+}
+
+/* ============================================================
+   Details / Micro-text — Figma-aligned (node 4428-10016)
+   Inter / 10px / lh=1.3
+   Light=weight 500 | Dark=weight 700
+   Use class .text-details or .text-details-dark
+   ============================================================ */
+.text-details {
+    font-family: var(--body-font-family);
+    font-weight: var(--details-font-weight-light);
+    font-size: var(--details-font-size);
+    line-height: var(--details-line-height);
+}
+
+.text-details-dark {
+    font-family: var(--body-font-family);
+    font-weight: var(--details-font-weight-dark);
+    font-size: var(--details-font-size);
+    line-height: var(--details-line-height);
+}
+
+/* ============================================================
+   Testimonial / Quote — Figma-aligned (node 5217-3113/3114)
+   Montserrat ultra-thin / no letter-spacing
+   Mobile: weight=250, 24px, lh=1.333 | Desktop: weight=100, 32px, lh=1.25
+   Use class .text-testimonial on blockquote or p elements.
+   NOTE: requires Montserrat 100–300 weight range woff2 (see fonts.scss)
+   ============================================================ */
+.text-testimonial {
+    font-family: var(--testimonial-font-family);
+    font-weight: var(--testimonial-font-weight);
+    font-size: var(--testimonial-font-size);
+    line-height: var(--testimonial-line-height);
+    letter-spacing: normal;
+    font-style: normal;
+}
+
 /* ============================================================
    Links — Figma-aligned (node 5366-7069, style-yn409d)
    Inline Link:      Inter 400 / 16px / 1.5em / -2% letter-spacing / underline
@@ -750,4 +835,4 @@ del {
 
 div:empty {
     display: none;
-}
\ No newline at end of file
+}
